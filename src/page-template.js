module.exports = (manager, engineer, intern, employee) => {
    return `
    <!DOCTYPE html>
    <html lang="en">
   <head>
     <title>Team</title>
    </head>
   <body> 
    <div>${res.manager}</div>
    <div>${res.engineer}</div>
    <div>${res.intern}</div>
    <div>${res.employee}</div>
    </body>
    </html>   
     `;
};